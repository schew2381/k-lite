# Lockdown: a may talk ONLY to b. The wildcard DENY covers every
# destination, and the except list carves b back out of it.
apiVersion: klite/v1
kind: NetworkPolicy
metadata:
  name: lockdown-a
spec:
  action: DENY
  rules:
    - from: a
      to: "*"
      except: [b]
